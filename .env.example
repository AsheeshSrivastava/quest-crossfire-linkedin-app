# =============================================================================
# Supabase Configuration
# =============================================================================
# Get these from: https://app.supabase.com/project/_/settings/api
# Required for: User session storage, LinkedIn token persistence

SUPABASE_URL=https://your-project-id.supabase.co
# Example: https://abcdefghijklmn.supabase.co

SUPABASE_ANON_KEY=your-supabase-anon-key-here
# Public anonymous key (safe to expose in frontend)

SUPABASE_SERVICE_KEY=your-supabase-service-role-key-here
# ⚠️ SECRET: Never expose in frontend! Backend use only.


# =============================================================================
# LinkedIn OAuth Configuration
# =============================================================================
# Get these from: https://www.linkedin.com/developers/apps
# Required for: LinkedIn authentication and post publishing

LINKEDIN_CLIENT_ID=your-linkedin-client-id
# Example: 78abcdefg123456

LINKEDIN_CLIENT_SECRET=your-linkedin-client-secret
# ⚠️ SECRET: Keep confidential! Backend use only.

LINKEDIN_REDIRECT_URI=http://localhost:3000/api/auth/linkedin/callback
# For local development use: http://localhost:3000/api/auth/linkedin/callback
# For production use: https://your-domain.com/api/auth/linkedin/callback
# Must match EXACTLY what's configured in LinkedIn Developer Console


# =============================================================================
# n8n Workflow Webhooks
# =============================================================================
# Get these from: Your n8n instance > Workflow > Webhook node
# Required for: AI post generation and LinkedIn publishing

N8N_GENERATE_WEBHOOK=https://your-n8n-instance.app.n8n.cloud/webhook/linkedin-generate
# Webhook for AI post generation workflow

N8N_PUBLISH_WEBHOOK=https://your-n8n-instance.app.n8n.cloud/webhook/linkedin-publish
# Webhook for LinkedIn publishing workflow


# =============================================================================
# JWT Secret
# =============================================================================
# Generate a secure random string for session tokens
# Use: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
# ⚠️ CRITICAL: Change this in production! Never use default values.

JWT_SECRET=your-random-secret-string-change-this-in-production
# Example: a3f7b9c2e1d4f6a8b0c9e2f5a7b1c3d5e8f2a6b9c4e7f1a3b6c9d2e5f8a1b4c7


# =============================================================================
# Base URL
# =============================================================================
# The public URL of your application
# Local development: http://localhost:3000
# Production: https://your-domain.com

NEXT_PUBLIC_BASE_URL=http://localhost:3000
# For production deployment, update to: https://linkedin.questandcrossfire.com


# =============================================================================
# DEPLOYMENT NOTES
# =============================================================================
# 1. Copy this file to .env.local for local development
# 2. Never commit .env.local to git (already in .gitignore)
# 3. For Vercel deployment, add these as Environment Variables in dashboard
# 4. Rotate JWT_SECRET regularly (monthly recommended)
# 5. Use different Supabase projects for dev/staging/production
# 6. Test LinkedIn OAuth with test account before production
#
# See DEPLOYMENT-GUIDE.md for detailed instructions
